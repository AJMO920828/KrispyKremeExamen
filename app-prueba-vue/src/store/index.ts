import { createStore } from "vuex";
import { loading } from "./loading.store"; // Importa el módulo de loading

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    loading,
  },
});
